To jest build zrobiony bez katalogu HSCP.

Strumień z "cmake ../" przekierowany do cmake_log.log i cmake_err_log.log
Strumień z "make install -j 4" > make_log.log i make_err_log.log

CMAKE_VERSION 7.3.0

Zakomentowany fragment kodu w ../CMakeLists.txt, bez tego jakoś się nie kompiluje.

Kompilacja z katalogiem HSCP znajdzie się w ../build_hscp
